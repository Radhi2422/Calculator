const OPERATORS = {
    ADD: "+",
    SUBTRACT: "-",
    MULTIPLY: "*",
    DIVIDE: "/"
};

const MAX_DIGITS = 12;

const ERROR_MESSAGES = {
    DIVIDE_BY_ZERO: "Cannot divide by zero",
    INVALID_INPUT: "Invalid Input"
};